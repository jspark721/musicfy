let animateDescBox = function() {
    let box = document.getElementsByClassName('desc');

    let revealFirstBox = function() {
        box[0].style.opacity = 1;
        box[0].style.transform = "scaleX(1) translateY(0)";
    }
}